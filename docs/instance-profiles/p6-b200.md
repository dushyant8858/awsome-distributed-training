# p6-b200 Instance Family — NVIDIA B200

> Covers: p6-b200.48xlarge

## Hardware at a Glance

| Spec | p6-b200.48xlarge |
|------|------------------|
| GPU | NVIDIA B200 (Blackwell) |
| GPU count | 8 |
| VRAM per GPU | 179 GB HBM3e |
| Memory bandwidth per GPU | 8,000 GB/s |
| BF16 TFLOPS per GPU | 2,250 (dense) / 4,500 (sparse) |
| FP8 TFLOPS per GPU | 4,500 (dense) / 9,000 (sparse) |
| FP4 TFLOPS per GPU | 9,000 (dense) / 18,000 (sparse) |
| TF32 TFLOPS per GPU | 1,100 (dense) / 2,200 (sparse) |
| GPU interconnect | NVSwitch, NVLink 5.0 (1,800 GB/s per GPU) |
| Total NVLink aggregate BW | 14.4 TB/s |
| GPUDirect RDMA | Yes |
| EFA | 8 adapters (EFAv4) |
| Network bandwidth | 3,200 Gbps |
| vCPUs | 192 (Intel Xeon Emerald Rapids) |
| System memory | 2,048 GiB |
| Local NVMe | 8 x 3.84 TB |

## Key Characteristics

- **Blackwell architecture** — 5th-gen Tensor Cores, 2nd-gen Transformer Engine,
  FP4/FP6 precision support; dual-die design with 208B transistors
- **179 GB HBM3e per GPU** — 27% more VRAM than H200 (141 GB); 8 TB/s memory
  bandwidth is 67% higher than H200's 4.8 TB/s
- **NVLink 5.0** — 1,800 GB/s per GPU (2x the bandwidth of H100/H200's NVLink
  4.0); 14.4 TB/s total bisection bandwidth across 8 GPUs
- **2.25 PFLOPS BF16** per GPU (dense) — ~2.3x the sparse BF16 compute of H100/H200
- **EFAv4** — latest generation with improved latency and throughput; 8
  adapters provide 3,200 Gbps aggregate

## Distributed Training Considerations

- **Max model size**: ~50B+ without offloading (BF16, TP=8); 179 GB per GPU
  is sufficient for most models without memory optimization
- **FP4 training**: B200's FP4 capability (9 PFLOPS/GPU dense) enables new
  generation of quantized training workflows with minimal accuracy loss
- **NVLink 5.0 advantage**: 2x the intra-node bandwidth of Hopper; enables
  more efficient tensor parallelism and larger TP degrees
- **Generational leap**: ~2.3x sparse BF16 TFLOPS, 1.27x VRAM, 1.67x memory
  BW, 2x NVLink BW vs H200

## NCCL / EFA Configuration

> For complete NCCL/EFA tuning guidance, see the
> [EFA Cheatsheet](../../1.architectures/efa-cheatsheet.md).

```bash
# p6-b200 supports GPUDirect RDMA
export FI_EFA_USE_DEVICE_RDMA=1

# Standard EFA settings
export FI_PROVIDER=efa
export NCCL_SOCKET_IFNAME=^docker,lo,veth
```

## Comparison with p5en

| Aspect | p6-b200.48xlarge | p5en.48xlarge |
|--------|------------------|---------------|
| GPU | B200 179 GB HBM3e | H200 141 GB HBM3e |
| BF16 TFLOPS/GPU | 2,250 (dense) | 990 (dense) |
| Memory bandwidth | 8,000 GB/s | 4,800 GB/s |
| NVLink per GPU | 1,800 GB/s (v5) | 900 GB/s (v4) |
| EFA | 8 adapters (EFAv4) | 16 adapters (EFAv3) |
| PCIe | Gen5 (Intel Emerald Rapids) | Gen5 (Intel Sapphire Rapids) |
